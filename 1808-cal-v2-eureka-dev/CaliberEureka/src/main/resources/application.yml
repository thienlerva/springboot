spring:
  application:
    name: eureka

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_HOME}
    register-with-eureka: false
    fetch-registry: false


logging:
  level:
    com:
      netflix:
        eureka: ALL
server:
  port: 8761


